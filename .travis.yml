language: php

php:
- '5.6'
- '7'

before_script:
  - composer self-update
  - composer install --dev

script:
  - ./vendor/bin/phpcs --standard=psr2 -n --ignore=vendor .
  - ./vendor/bin/phpunit

after_script:
  - ./codeclimate.sh